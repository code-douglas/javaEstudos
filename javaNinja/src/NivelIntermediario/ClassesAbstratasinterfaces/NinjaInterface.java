package NivelIntermediario.ClassesAbstratasinterfaces;

public interface NinjaInterface {
  // Obrigatoriamente abstrato, sendo necessário instanciar;
  // Não pode setar atributos depois, tem que setar no momento que a variavel 
  // for criada; Em resumo, deixaremos a interface para métodos somente;
  public void jogarShuriken();
}
